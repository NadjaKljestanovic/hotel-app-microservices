server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/hotel_db
    username: hotel
    password: hotel

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
